name: audioguia_web
description: "Panel de administración para la Audioguía de Martos."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_quill: ^11.5.0
  supabase_flutter: ^2.12.4
  go_router: ^17.1.0
  http: ^1.2.2
  provider: ^6.1.2
  flutter_dotenv: ^6.0.1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
